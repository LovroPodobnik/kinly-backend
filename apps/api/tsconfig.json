{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    // Removed rootDir and outDir - Bun handles bundling
    // rootDir: "src" breaks imports from workspace packages (@my-app/auth, etc.)
  },
  "include": ["src"]
}
